1.0

➡️ Group to include

🚫 Group to exclude

Textbox for optional policy name filter

A big multi-line output log

It will:

Loop over Device Configuration Policies + Settings Catalog + Compliance Policies

Remove the exclude group from included if it exists

Add the exclude group as an exclusion if not already

Likewise add the include group if not already

All with logs in the GUI.